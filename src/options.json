{
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "context": {
      "description": "A string indicating the root of your files.",
      "type": "string"
    },
    "lintDirtyModulesOnly": {
      "description": "Lint only changed files, skip lint on start.",
      "type": "boolean"
    },
    "linters": {
      "description": "The linters to run, each with the options only it understands.",
      "type": "array",
      "minItems": 1
    },
    "use": {
      "description": "The linter to run, either the name of a built-in one or a linter adapter.",
      "anyOf": [{ "type": "string" }, { "type": "object" }]
    }
  }
}
